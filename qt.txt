Qmake – собственный компилятор QT.
До QT пользовались так как c++ абсолютно неудобен для написания приложений windows api, mfc(Microsoft Foundation Classes) - библиотеки для работы с графикой на с++.

QT начиная с версии 3 : windows(xp, vista and » ),Linux, Mac OS X, Solaris, AIX, Irix, NetBSD, OpenBSD, HP-VX, Free-BSD.

Qt используют adobe, IBM, NASA, Boeing, Simens, Sony Xerox и пр и пр.
У QT быстрая скорость работы и удобность пользования.

С++ - компилиируемый язык в отличии от джавы.
Скайп ,фотошоп, множество карт, VLC написаны на QT.

2-3 мерная графика OpenGl и direct x – две вещи, на которых может поддерживаться графика.
Qt полностью объектно -ориентированный. Мультиплатформенность ,использование формата XML, полность поддерживает библиотеку контейнеров STL (collection in JAVA), поддержка стандартых потоков ввода-вывода, модуль работы с базой данных (oracle, microsoft, postgresql и многие другие).

Плагины QT:
QT используется не только для с++ - QT для джава, перла, с# и мн др.

Qt script – язык сценариев.

Писать будем на
1.Eclipse + QT plagin
2. QT Creator включает в себя: qt designer, qt assistent – ide с примерами на все случаи жизни.

First QT program:

#include<QtGui>//модуль для работы с графикой
int main(int args, char ** argv){
Qapplication app(args, argv);
Qlabel lbl(“Hello World!”);
lbl.show();
return app.exec();
}

Объект Qapplication взят из QtGui. Любой объект работающий с графикой содержит в себе один такой объект.
app.exec(); - следит за тем, чтобы приложение работало, пока в основном потоке есть хотя бы один объект.
По-умолчанию объекты не видимы. Объект в потоке появляется при вызове.

Модули QT:

QtCore – классы, не связанные с граф интерфейсом.
QtGui
QtNetwork
QtOpenGL
QtSql
QtSvg – векторня графика
QtXml
Qt3Support
QtScript
Phonon
QtWebKit
QtTest
QtScriptTools

базовый класс.
Qobject-> 1.Поддерживает сигналы и слоты. 2.Объединение объектов в иерархии.3 События и механизмы их фильтрации. 4. Организация объектных иерархий. 5. Метаобъектная информация. 6. ПРиведение типов. 7. Свойства 8 Таймер.
